# ── Node ────────────────────────────────────────────────
node_modules/

# ── Secrets (never commit) ──────────────────────────────
.env
.env.local
.env.*.local

# ── Logs & runtime files ────────────────────────────────
*.log
server.pid

# ── User-generated content (added at runtime) ───────────
public/uploads/

# ── Local tooling / scratch ─────────────────────────────
.freebuff/
.claude/
tmp-*
_*.ps1
_*.sh
npm-dev-log.txt
problem*.png
.freebuff_checkout_new.html
check-out.txt
checks-out.txt
live-out.txt
audit-out.txt
boot-time.txt
phase2-out.txt
remote-check.txt
remote-err.txt

# ── OS ──────────────────────────────────────────────────
.DS_Store
Thumbs.db